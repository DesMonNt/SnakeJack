class Main {
    function void main() {
        var Food f;
        var Point p;
        let f = Food.new(16);
        let p = f.getPosition();

        while (true){
            do f.changePosition();
            do f.draw();
        }

        return; 
        }
}
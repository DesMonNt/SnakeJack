class Main {
    function void main() {
        var Game game;

        while (true){
            let game = Game.new(16);
            do game.run();
            do game.dispose();

            // gameover

            while(~(Keyboard.keyPressed() = 82)){ 
            }
        
        }
        return; 
    }
}